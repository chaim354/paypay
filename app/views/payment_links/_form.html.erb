<%= form_with(model: payment_link, class: "contents") do |form| %>
  <% if payment_link.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(payment_link.errors.count, "error") %> prohibited this payment_link from being saved:</h2>

      <ul class="list-disc ml-6">
        <% payment_link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :paymentplatform_id %>
    <%
=begin%>
 <%= form.collection_select :paymentplatform_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": payment_link.errors[:paymentplatform_id].none?, "border-red-400 focus:outline-red-600": payment_link.errors[:paymentplatform_id].any?}] %> 
<%
=end%>
    <%= form.collection_select :payment_platform_id, @payment_platforms, :id, :name, prompt: "Select Payment Platform" %>
  </div>

  <div class="my-5">
    <%= form.label :payid %>
    <%= form.text_field :payid, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": payment_link.errors[:payid].none?, "border-red-400 focus:outline-red-600": payment_link.errors[:payid].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
